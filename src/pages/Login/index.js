import React from 'react';
import { Layout } from '../../components/common';
import {LoginForm} from '../../components/Login';

const Login = (props) => (<Layout>
	<LoginForm />
</Layout>);

export default Login;