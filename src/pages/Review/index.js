import React from 'react';
import { Layout } from '../../components/common';
import {Review as ReviewComponent} from '../../components/Review';

const Review = (props) => (<Layout>
	<ReviewComponent/>
</Layout>);

export default Review;