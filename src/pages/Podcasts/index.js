import React from 'react';
import { Layout } from '../../components/common';
import { PodcastFeed } from '../../components/Podcasts';

const Podcasts = (props) => (<Layout>
	<PodcastFeed />
</Layout>);

export default Podcasts;